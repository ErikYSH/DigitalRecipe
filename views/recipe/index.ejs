<%- include('../partials/head.ejs') %>
<table id="list">
  <thead>
    <div>
        
          </svg>
        <div class="dropdown">
            <button  class="btn btn-secondary dropdown-toggle" style="background-color: #324456;margin-bottom: 10px; margin-top: 10px""button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
              CATEGORIES
            </button>
            </a>
            <ul class="dropdown-menu text-small shadow" aria-labelledby="dropdownNavLink" >
           
              <li><a class="dropdown-item active" href="#" aria-current="page">All</a></li>
              <% categories.forEach(category => {%>
              <li>
                <a class="dropdown-item" href="#"><%= category.name %></a> 
              </li>
                <% }) %>
            </ul>
          </div>
    </div> 


      <div class="d-grid gap-2">
        <button class="btn btn-secondary" type="button" style="background-color: white; margin-bottom: 10px; margin-top: 10px; color: #324456; font-family: rubik;">ADD NEW RECIPE + </button>
      </div>

    <div class="recipecards">
        <div class="card-group">
          <% recipes.forEach(recipe => {%> 
            <div class="card" style="margin-right: 10px"> 
              <img src="/images/chocolatecake.png" class="card-img-top" alt="...">
              <div class="card-body">
                <h5 class="card-title" style="font-family: rubik"><%= recipe.name %> </h5>
                <a href="/recipe/<%= recipe._id %>" class="btn btn-secondary" style="background-color: white; margin-bottom: 10px; margin-top: 10px; color: #324456; font-family: rubik;">Recipe</a>
              </div>
            </div>
            <%}) %> 
            
              </div>
            </div>
          </div>
    </div>
  </thead>

