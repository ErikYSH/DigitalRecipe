<%- include('../partials/head.ejs') %>
<table id="list">
  <thead>
<div class="container-sm" style="margin-bottom: 10px;">
    <div>
        <div class="dropdown">
            <button  class="btn btn-secondary dropdown-toggle" style="background-color: #324456;margin-left:10px; margin-bottom: 2px; margin-top: 10px""button" id="dropdownMenu2" data-bs-toggle="dropdown" aria-expanded="false">
              CATEGORIES
            </button>
            </a>
            <ul class="dropdown-menu text-small shadow" aria-labelledby="dropdownNavLink" >
           
              <li><a class="dropdown-item active" href="#" aria-current="page">All</a></li>
              <% categories.forEach(category => {%>
              <li>
                <a class="dropdown-item" href="#"><%= category.name %></a> 
              </li>
                <% }) %>
            </ul>
          </div>
    </div> 

  
    
      <div class="d-grid gap-2">
        <a href="/recipe/newRecipe">
            <button class="btn btn-secondary" type="button" style="background-color: white; margin-left: 10px; margin-bottom: 10px; margin-top: 10px; color: #324456; font-family: rubik;">ADD NEW RECIPE + </button>
          </a>
      </div>
    


      <div class="container">
        <div class="row row-cols-3">
          <% recipes.forEach(recipe => {%> 
            <div class="col">
            <div class="card" style="margin-bottom: 10px; margin-top: 10px;"> 
              <img src="<%= recipe.ingredient_img %> " class="card-img-top" alt="<%= recipe.name %> ">
              <div class="card-body">
                <h5 class="card-title" style="font-family: rubik"><%= recipe.name %> </h5>
                <a href="/recipe/<%= recipe._id %>" class="btn btn-secondary" style="background-color: white; margin-bottom: 10px; margin-top: 10px; color: #324456; font-family: rubik;">RECIPE</a>
              </div>
            </div>
            </div>
          <%}) %> 
        </div>
      </div>




  </div>
</div>  



